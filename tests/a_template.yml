AWSTemplateFormatVersion: '2010-09-09'

Parameters:
  ExampleParamA:
    Type: String
  ExampleParamB:
    Type: String
    Default: "b default"

Resources:
  EcrRepo:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: test-ecr-repo
